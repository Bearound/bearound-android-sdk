<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools" xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bluetooth permissions for Android 11 and below -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Bluetooth scan permission for Android 12+ (API 31+), WITH neverForLocation.
         The assertion is what makes Bluetooth-only detection possible: without it Android
         12+ classifies the scan as location-deriving and silently withholds EVERY scan
         result unless the app also holds ACCESS_FINE_LOCATION (verified on-device: an
         unfiltered scan delivered 0 results while system scanners saw 200+/min).
         Android's docs warn the assertion "can filter beacons out of the scan results";
         validated NOT to affect the Bearound signatures (0xBEAD service data + iBeacon
         frame) on One UI and HyperOS devices.
         MERGE WARNING: if ANY manifest in the merge declares BLUETOOTH_SCAN without this
         flag, the flag is dropped from the merged manifest and detection goes blind for
         apps without location. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />

    <!-- Encounter layer (device-to-device sightings), API 31+. ADVERTISE lets the host
         be seen by other SDK devices — identity travels in the advertisement itself
         (scan-response service data), so no connections and no BLUETOOTH_CONNECT are
         ever needed. Runtime-checked: a host that never requests it keeps every other
         feature and the layer degrades to receive-only — the SDK never throws. -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"
        tools:targetApi="s" />

    <!-- Location — declared on ALL versions. On API ≤ 30 it is the legacy BLE-scan gate
         (detection does not work without it there); on API 31+ detection works with
         Bluetooth alone (neverForLocation above), and granting location is recommended
         for maximum OEM coverage. Foreground only — ACCESS_BACKGROUND_LOCATION is
         intentionally NOT declared, so hosts need a standard Play Data Safety "Location"
         declaration but NOT the background-location video review. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Internet for API communication -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Wi-Fi state (normal permission, granted at install, no user prompt) — required by
         WifiManager to read the connected access point and the system's cached scan
         results. Without it the SDK reports no Wi-Fi observations at all. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Nearby Wi-Fi devices (Android 13+). Reading the neighbouring access points needs
         ACCESS_FINE_LOCATION on every version OR this permission on API 33+; either one
         is enough, and the SDK checks both at runtime. Declared WITHOUT neverForLocation
         on purpose: the observations exist precisely to be associated with a position,
         so asserting otherwise would be false.
         NOT a hard requirement — a host that never grants it simply sends no `wifis[]`,
         and every other SDK feature behaves exactly as before. The SDK never prompts. -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        tools:targetApi="tiramisu" />

    <!-- Refreshes the system Wi-Fi scan cache (normal permission, granted at install,
         no user prompt). Product decision: on idle devices nothing else refreshes the
         cache and observations decay to just the connected AP — the SDK nudges a scan
         periodically while scanning in foreground, inside Android's own 4-per-2-minutes
         throttle. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Wi-Fi hardware, required="false" for the same reason as Bluetooth below: this
         manifest merges into the host's, and a required feature would hide client apps
         from Play Store devices that lack it. -->
    <uses-feature android:name="android.hardware.wifi" android:required="false" />

    <!-- Notifications permission (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Google Advertising ID (AAID). Normal permission, granted at install with no user
         prompt — but REQUIRED from targetSdk 33+: without it the platform hands back a
         zeroed id even when the user allows ad personalisation.
         The user's own choice still wins: opting out in system settings zeroes the id and
         the SDK reports none. Declaring this obliges the host to tick "Device or other IDs"
         in the Play Data Safety form; a host that must NOT collect it (apps for children)
         can strip it with tools:node="remove" and the SDK degrades to reporting no id. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    
    <!-- Boot completed permission for restart scanning after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- NO USE_EXACT_ALARM / SCHEDULE_EXACT_ALARM: the SDK is not a calendar or
         alarm-clock app, so it does not qualify for exact alarms on Google Play
         (the Play Console requires removing them, otherwise it demands the "Exact
         alarms" declaration). The scan watchdog uses an INEXACT alarm
         (setAndAllowWhileIdle), which runs in Doze without any special permission —
         minute-level precision is irrelevant for a WorkManager safety net. -->

    <!-- Foreground service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- Hardware features. required="false" ON PURPOSE: this manifest merges into the HOST
         app's, and uses-feature merges with OR (true wins) — required="true" here would make
         every client app invisible on the Play Store for devices without BLE, even when the
         app has plenty of non-beacon functionality. The SDK degrades gracefully at runtime
         (adapter == null → no scanning). -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <application>
        <!-- BroadcastReceiver for PendingIntent-based BLE scan (API 26+) -->
        <receiver
            android:name="io.bearound.sdk.background.BluetoothScanReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="io.bearound.sdk.ACTION_BLUETOOTH_SCAN" />
            </intent-filter>
        </receiver>
        
        <!-- Watchdog Receiver for AlarmManager + Boot Completed -->
        <receiver 
            android:name="io.bearound.sdk.background.ScanWatchdogReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="io.bearound.sdk.ACTION_WATCHDOG" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Foreground service to keep process alive for BLE scanning (opt-in) -->
        <service
            android:name="io.bearound.sdk.background.BeaconScanService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
    </application>

</manifest>
